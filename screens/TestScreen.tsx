import React from 'react';
import { View, Text } from 'react-native';

function TestScreen(props: any) {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text> this is Test screenn</Text>
        </View>
    );
}

export default TestScreen;